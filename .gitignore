# macOS
.DS_Store

# Firebase CLI artifacts (per-project caches and logs)
.firebase/
firebase-debug.log*
firebase-debug.*.log*
.firebaserc.backup

# Submission secrets — included in the ZIP, NEVER committed to git
secrets.txt
